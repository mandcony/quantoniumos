[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: marks tests that require external model downloads or long-running operations
    slow: marks tests that are long-running (deselect with -m "not slow")
