# QuantoniumOS Requirements
numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.4.0
pandas>=1.3.0

# Optional dependencies for extended functionality
sympy>=1.8
jupyter>=1.0.0

# Development dependencies
pytest>=6.0.0
black>=21.0.0
flake8>=3.9.0

# Hugging Face AI/ML Dependencies
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0
accelerate>=0.24.0
peft>=0.6.0
trl>=0.7.0
bitsandbytes>=0.41.0  # For quantization
wandb>=0.15.0  # Optional: for experiment tracking

# Image Generation Dependencies
diffusers>=0.21.0  # Stable Diffusion and other generative models
pillow>=9.0.0  # PIL for image handling
controlnet-aux>=0.0.6  # Optional: for ControlNet support
xformers>=0.0.20  # Optional: memory efficient attention (NVIDIA only)

# Assembly interface (platform specific)  
# Windows: Microsoft Visual C++ Build Tools
# Linux: GCC compiler toolchain

# Note: Large trained model files (1.9GB) are stored locally in ai/training/models/
# Contains: fixed_fine_tuned_model/ and quantum_fine_tuned_model/
# These models are preserved but excluded from Git due to GitHub size limits
