#ifndef MAIN_H
#define MAIN_H

// This could contain declarations for functions in main.cpp if it were a library.
// For an executable's main.cpp, a header is not strictly necessary unless other
// files in the project need to call functions from it.

#endif // MAIN_H
